<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        {{#if description}}
            <meta description="{{ description }}" />
        {{/if}}
        <title>{{ title }}</title>
    </head>

